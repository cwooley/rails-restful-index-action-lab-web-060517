<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Students</title>
  </head>
  <body>
    <% @students.each do |student| %>
      <p>Name: <%= student.first_name + " " + student.last_name %></p>
    <% end %>
  </body>
</html>
